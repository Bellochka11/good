package GeekBrains.Lesson_2;

import java.lang.System.Logger;
import java.util.logging.ConsoleHandler;
import java.util.logging.SimpleFormatter;

public class lec_3 {
    public static void main(String[] args) {
        // //Использование
        //Logger logger = Logger.getLogger();
        // //Уровни важности
        // //INFO, DEBUG, ERROR, WARNING и др.
        // //Вывод
        // ConsoleHandler info = new ConsoleHandler();
        // log.addHandler(info);
        // //Формат вывода: структурированный, абы как*
        // XMLFormatter, SimpleFormatter
        //Logger logger = Logger.getLogger(lec_3.class.getName()); //создали логирование и указали имя класса для него
        // //logger.setLevel(Level.INFO);
        // ConsoleHandler ch = new ConsoleHandler();//создали хэндлер ошибки будут выподать в терминал
        // logger.addHandler(ch);//куда отправляются сообщения
        // SimpleFormatter sFormat = new SimpleFormatter(); //формат запись или вывод сообщений
        // //ch.setFormatter(sFormat);
        // logger.log(Level.WARNING, "Тестовое логирование");//уровень сообщения и его текст
        // logger.info("Тестовое логирование");

    }
}
